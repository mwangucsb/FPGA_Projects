`timescale 1ns / 1ps

module main_test_tb;

    //============================================================
    // Testbench signals
    //============================================================

    logic       clk;
    logic       reset;
    logic [7:0] sw;
    logic       rd;
    logic       wr;

    logic [7:0] an;
    logic [7:0] sseg;


    //============================================================
    // Instantiate DUT
    //============================================================

    main_test dut (
        .clk(clk),
        .reset(reset),
        .sw(sw),
        .rd(rd),
        .wr(wr),
        .an(an),
        .sseg(sseg)
    );


    //============================================================
    // Clock
    //
    // 10 ns period = 100 MHz
    //============================================================

    always #5 clk = ~clk;


    //============================================================
    // Test
    //============================================================

    initial begin

        // Initial values
        clk   = 0;
        reset = 1;
        sw    = 8'd0;
        rd    = 0;
        wr    = 0;

        //========================================================
        // RESET
        //========================================================

        #20;

        reset = 0;

        #10;

        $display("----------------------------------------");
        $display("RESET COMPLETE");
        $display("count = %0d", dut.count);
        $display("empty = %0b", dut.empty);
        $display("full  = %0b", dut.full);
        $display("----------------------------------------");


        //========================================================
        // WRITE 1
        //========================================================

        sw = 8'd12;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 2
        //========================================================

        sw = 8'd25;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 3
        //========================================================

        sw = 8'd37;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 4
        //========================================================

        sw = 8'd64;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 5
        //========================================================

        sw = 8'd89;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 6
        //========================================================

        sw = 8'd123;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 7
        //========================================================

        sw = 8'd200;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d",
                 sw, dut.count);


        //========================================================
        // WRITE 8
        //========================================================

        sw = 8'd255;

        #5;
        wr = 1;

        #10;
        wr = 0;

        #5;

        $display("WRITE: %0d | count = %0d | full = %0b",
                 sw, dut.count, dut.full);


        //========================================================
        // WAIT
        //========================================================

        #20;


        //========================================================
        // READ 1
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 2
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 3
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 4
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 5
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 6
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 7
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d",
                 dut.r_data, dut.count);


        //========================================================
        // READ 8
        //========================================================

        rd = 1;

        #10;
        rd = 0;

        #5;

        $display("READ: r_data = %0d | count = %0d | empty = %0b",
                 dut.r_data, dut.count, dut.empty);


        //========================================================
        // FINISH
        //========================================================

        #20;

        $display("----------------------------------------");
        $display("TEST COMPLETE");
        $display("----------------------------------------");

        $finish;

    end

endmodule